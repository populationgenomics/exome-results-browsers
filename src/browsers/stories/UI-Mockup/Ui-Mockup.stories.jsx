import React from 'react'

import { UIMockup } from './UI-Mockup'

export default {
  title: 'Components/UI',
  component: UIMockup,
}

const Template = (args) => <UIMockup {...args} />

export const main = Template.bind({})
